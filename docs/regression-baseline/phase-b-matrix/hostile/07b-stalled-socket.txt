================================================================================
GATE M Part 1 (Phase B) — profile: hostile — case 07b-stalled-socket: a socket that stops producing
================================================================================

PURPOSE   Every read carries a stall timeout, so a wedged endpoint cannot wedge the app.
SUBJECT   Vela's own provider stack (vela-providers::OpenAiCompatibleProvider)
ENDPOINT  tests/harness/mock-provider, profile `hostile`, a real OS process on loopback
HONESTY   VERIFIED-BY-FAKE. The endpoint is a deterministic mock; no model produced
          any byte below. Nothing here is evidence about a real model.


---- the stream (endpoint configured with --chunk-delay 4000, stall budget 800 ms) 

  >>> exchange 1 — POST http://127.0.0.1:34207/v1/chat/completions
      content-type: application/json
      accept: text/event-stream
      (no authorization header)
      --- request body (125 bytes) ---
      {"messages":[{"content":"hello","role":"user"}],"model":"mock-hostile","stream":true,"stream_options":{"include_usage":true}}
      <<< HTTP 200
      content-type: text/event-stream; charset=utf-8
      transfer-encoding: chunked
      --- response body (190 bytes, read to end) ---
      data: {"id":"chatcmpl-mock-c734be66","object":"chat.completion.chunk","created":1700000000,"model":"mock-hostile","choices":[{"index":0,"delta":{"role":"assistant"},"finish_reason":null}]}
      

---- what Vela produced ------------------------------------------------------
  outcome                                Ok(Err(Transport { failure: Stalled, detail: "no data for 800 ms" }))
  error                                  Some(Transport { failure: Stalled, detail: "no data for 800 ms" })
  WALL CLOCK                             810.92235ms

---- assertions --------------------------------------------------------------
  [PASS] a stalled socket is abandoned rather than awaited forever
        810.92235ms
  [PASS] and the abandonment is an explicit error, not an empty success
        Some(true)

================================================================================
VERDICT  2 assertion(s), 0 failure(s)
================================================================================
