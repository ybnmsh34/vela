/**
 * The diagnostics feature: the one switch that decides whether the `trace` id
 * on a failed turn points at anything.
 */

export { DebugLogSwitch } from './DebugLogSwitch';
export { useDebugLog, type DebugLogController } from './use-debug-log';
