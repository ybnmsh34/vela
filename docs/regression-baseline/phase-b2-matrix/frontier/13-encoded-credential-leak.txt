================================================================================
GATE M Part 1 (Phase B2) — profile: frontier — case 13-encoded-credential-leak: a credential the endpoint spells differently is still a credential
================================================================================

PURPOSE   Round 3's scrub removes the spelling it was shown. A decoder's whole job is to turn one spelling into another. This case drives the three encodings the round-4 brief names, and then four nobody named — because every round of this piece has died on a surface the previous round's tests did not cover.
SUBJECT   Vela's own provider stack (vela-providers::OpenAiCompatibleProvider)
ENDPOINT  tests/harness/mock-provider, profile `frontier`, a real OS process on loopback
HONESTY   VERIFIED-BY-FAKE. The endpoint is a deterministic mock; no model produced
          any byte below. Nothing here is evidence about a real model.

  THE SHAPE DRIVEN IS FINDING 2's: a 200 whose SSE stream carries an
  `{"error":{...}}` object, read frame by frame through `BodyStream::next_chunk` —
  and, beside it, a 400 read whole. Both bindings: the credential in the query string
  (percent-encoded on the wire, DECODED by any gateway that parses it) and the
  credential in an `x-api-key` header (raw, `/` and all).
  A spelling is judged two ways, and the transcript says which applies:
  READABLE          the key, or the percent form Vela itself wrote, is in the text
  RECONSTRUCTIBLE   it is not — but one pass of ordinary decoding gets back to it

---- every spelling, both bindings, both response shapes, both transports ----
    verbatim (control) · ?key= · HTTP 200 · stream() → clean
    verbatim (control) · ?key= · HTTP 200 · complete() → clean
    verbatim (control) · ?key= · HTTP 400 · stream() → clean
    verbatim (control) · ?key= · HTTP 400 · complete() → clean
    verbatim (control) · x-api-key · HTTP 200 · stream() → clean
    verbatim (control) · x-api-key · HTTP 200 · complete() → clean
    verbatim (control) · x-api-key · HTTP 400 · stream() → clean
    verbatim (control) · x-api-key · HTTP 400 · complete() → clean
    PHP json_encode: \/ · ?key= · HTTP 200 · stream() → clean
    PHP json_encode: \/ · ?key= · HTTP 200 · complete() → clean
    PHP json_encode: \/ · ?key= · HTTP 400 · stream() → clean
    PHP json_encode: \/ · ?key= · HTTP 400 · complete() → clean
    PHP json_encode: \/ · x-api-key · HTTP 200 · stream() → clean
    PHP json_encode: \/ · x-api-key · HTTP 200 · complete() → clean
    PHP json_encode: \/ · x-api-key · HTTP 400 · stream() → clean
    PHP json_encode: \/ · x-api-key · HTTP 400 · complete() → clean
    every char as \uXXXX · ?key= · HTTP 200 · stream() → clean
    every char as \uXXXX · ?key= · HTTP 200 · complete() → clean
    every char as \uXXXX · ?key= · HTTP 400 · stream() → clean
    every char as \uXXXX · ?key= · HTTP 400 · complete() → clean
    every char as \uXXXX · x-api-key · HTTP 200 · stream() → clean
    every char as \uXXXX · x-api-key · HTTP 200 · complete() → clean
    every char as \uXXXX · x-api-key · HTTP 400 · stream() → clean
    every char as \uXXXX · x-api-key · HTTP 400 · complete() → clean
    percent-encoded, UPPERCASE hex · ?key= · HTTP 200 · stream() → clean
    percent-encoded, UPPERCASE hex · ?key= · HTTP 200 · complete() → clean
    percent-encoded, UPPERCASE hex · ?key= · HTTP 400 · stream() → clean
    percent-encoded, UPPERCASE hex · ?key= · HTTP 400 · complete() → clean
    percent-encoded, UPPERCASE hex · x-api-key · HTTP 200 · stream() → clean
    percent-encoded, UPPERCASE hex · x-api-key · HTTP 200 · complete() → clean
    percent-encoded, UPPERCASE hex · x-api-key · HTTP 400 · stream() → clean
    percent-encoded, UPPERCASE hex · x-api-key · HTTP 400 · complete() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · ?key= · HTTP 200 · stream() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · ?key= · HTTP 200 · complete() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · ?key= · HTTP 400 · stream() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · ?key= · HTTP 400 · complete() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · x-api-key · HTTP 200 · stream() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · x-api-key · HTTP 200 · complete() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · x-api-key · HTTP 400 · stream() → clean
    percent-encoded, lowercase hex   [UNBRIEFED] · x-api-key · HTTP 400 · complete() → clean
    percent-encoded, every byte      [UNBRIEFED] · ?key= · HTTP 200 · stream() → clean
    percent-encoded, every byte      [UNBRIEFED] · ?key= · HTTP 200 · complete() → clean
    percent-encoded, every byte      [UNBRIEFED] · ?key= · HTTP 400 · stream() → clean
    percent-encoded, every byte      [UNBRIEFED] · ?key= · HTTP 400 · complete() → clean
    percent-encoded, every byte      [UNBRIEFED] · x-api-key · HTTP 200 · stream() → clean
    percent-encoded, every byte      [UNBRIEFED] · x-api-key · HTTP 200 · complete() → clean
    percent-encoded, every byte      [UNBRIEFED] · x-api-key · HTTP 400 · stream() → clean
    percent-encoded, every byte      [UNBRIEFED] · x-api-key · HTTP 400 · complete() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · ?key= · HTTP 200 · stream() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · ?key= · HTTP 200 · complete() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · ?key= · HTTP 400 · stream() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · ?key= · HTTP 400 · complete() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · x-api-key · HTTP 200 · stream() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · x-api-key · HTTP 200 · complete() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · x-api-key · HTTP 400 · stream() → clean
    JSON-escaped twice: \\/          [UNBRIEFED] · x-api-key · HTTP 400 · complete() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · ?key= · HTTP 200 · stream() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · ?key= · HTTP 200 · complete() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · ?key= · HTTP 400 · stream() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · ?key= · HTTP 400 · complete() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · x-api-key · HTTP 200 · stream() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · x-api-key · HTTP 200 · complete() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · x-api-key · HTTP 400 · stream() → clean
    HTML entities: &#x2f;            [UNBRIEFED] · x-api-key · HTTP 400 · complete() → clean

---- what a leaking surface actually contains --------------------------------


---- assertions --------------------------------------------------------------
  [PASS] B2's inversion: the endpoint's message reaches the error on NO arm — the field it used to arrive in is gone
        0 of 64 arms carried the endpoint's own marker
  [PASS] and the peers really did send it — the premise moved upstream to the socket, where it is still answerable
        64 arm-equivalents saw the marker on the wire, of 64
  [PASS] STRONGEST: no string in any error's serde shape is unexplained by Vela's own closed vocabulary — not 'no credential', NO ENDPOINT TEXT AT ALL
        64 arms, 0 unexplained strings
  [PASS] the encoding peers put NO literal copy of the credential on the wire — the literal pass genuinely had nothing to match
        28 of 28 encoded peers
  [PASS] the BRIEFED encodings are all removed — verbatim, PHP's \/, \uXXXX, and the percent form Vela writes
        32 of 32 briefed arms clean
  [PASS] DELETION, not redaction — no `<redacted>` marker on the error surface either, because there is nothing there to mask
        0 of 64 arms carry the marker
  [PASS] NO SPELLING OF THE CREDENTIAL REACHES ANY SURFACE — Display, Debug, the serde JSON that crosses the IPC bridge, or the StreamEvent the UI is handed
        64 arms clean

================================================================================
VERDICT  7 assertion(s), 0 failure(s)
================================================================================
